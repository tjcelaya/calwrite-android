# CLAUDE.md

## Sketching new domain objects

When proposing a new domain object or a change to one, sketch it as plain code (Kotlin data
classes, enums, function signatures) rather than prose, bullet lists or diagrams. This applies
in every kind of session, terminal or not. Prose can explain the sketch, but it can't replace it.
Plans in `docs/plans/` follow the same rule.
